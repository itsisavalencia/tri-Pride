import "./Hero.scss"

const Hero = () => {
  return (
    <section className="hero">
        <h1 className="hero__title">Celebrating & Fostering Pride in the KW Region</h1>
        <button className="hero__button button">NEXT EVENT</button>
    </section>
  )
}

export default Hero